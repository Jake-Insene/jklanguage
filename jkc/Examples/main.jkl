
extern "jkl" fn write(Fd: UInt, Buff: const Byte[], Count: UInt);

fn Fact(Num: Int) Int {
	if (Num == 0) {
		return 1;
	}
	return Num * Fact(Num - 1);
}

fn Main() Int {
	//write(0U, "Hello!\n", 7U);
	return Fact(10);
}
