
fn Fact(Num: Int) Int {
    if(Num == 0) {
        return 1;
    }
    else {
        return Num * Fact(Num - 1);
    }
}

#Native
fn write(Fd: UInt, Buff: []const Byte, Count: UInt);

fn Main() Int {
    write(0U, "Hello World!\n", 15U);
    return Fact(10);
}
